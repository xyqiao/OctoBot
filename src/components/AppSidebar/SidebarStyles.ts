export const sidebarStyle = (theme: any) => ({
  width: 340,
  borderRadius: 0,
  p: 2.3,
  borderRight: `1px solid ${theme.appColors.border}`,
  backgroundColor: theme.appColors.sidebar,
  display: "flex",
  flexDirection: "column",
  minHeight: 0,
});
